/* Deutsch (de). Assembled from parts so each file stays reviewable.
   ui       interface strings, keyed by the English text
   plural   plural forms per Intl.PluralRules category (German: one, other)
   content  skills, guides, plans, badges and coaching text, keyed by id */
import ui from "./ui";
import plural from "./plural";
import content from "./content";
export default { ui, plural, content };
